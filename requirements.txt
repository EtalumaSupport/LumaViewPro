
Kivy==2.3.1
imagecodecs==2026.5.10
matplotlib==3.10.9
numpy==2.4.6
opencv-python-headless==4.13.0.92
pandas==3.0.3
pypylon==26.4.1
pyserial==3.5
scikit-image==0.26.0
scipy==1.17.1
tifffile==2026.5.15
platformdirs==4.9.6
# IDS peak Python bindings. ipl provides ConvertTo; icv is the IPL successor
# probed at runtime by drivers/idscamera.py; afl is the host auto-exposure/
# auto-gain engine (the U3-34L has no camera-side auto) -- probed at runtime
# today, the base of the planned host AE/AG routine, and its absence from a
# bench venv already cost a probe run. Pinned to the current PyPI latest.
# All four ship Windows + Linux wheels only; macOS dev envs run the tests
# against stubs.
ids-peak==1.15.0.0.8
ids-peak-ipl==1.17.2.0.7
ids-peak-icv==1.3.0.9744
ids-peak-afl==2.0.1.0.6
psutil==7.2.2
av==17.0.1

# FX2 (LVC LS560/LS620/LS720) USB driver bindings. Both packages are
# gated at runtime by drivers/fx2driver.py:_FX2_AVAILABLE — present
# without FX2 hardware, the driver no-ops with a single INFO log. The
# native libusb-1.0.dll must also be bundled at build time (see the
# PyInstaller spec); without it pyusb fails to load its libusb1
# backend on Windows.
pyusb==1.3.1
libusb1==3.4.0
