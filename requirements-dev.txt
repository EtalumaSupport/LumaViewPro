# Development and testing dependencies (not needed in production)
# Install with: pip install -r requirements-dev.txt

-r requirements.txt

# A pin's job is to make CI run what local verification actually ran. This one
# had drifted a full major behind the interpreter that produces every green
# suite result we quote, so CI would have run the suite under a pytest we have
# never used. Bump this whenever the development environment moves; a pin that
# disagrees with the environment that validates the suite is worse than none.
pytest==9.0.2
pyinstaller==6.19.0

# Linter + formatter. PINNED EXACTLY, and the pin is load-bearing: ruff
# changes its rule set and its formatting between releases, so a floating
# version turns CI red on a commit that touched nothing. Verified clean
# repo-wide at this version -- `ruff check .` passes and all 456 files are
# already formatted -- so a CI failure here means the change, not the tool.
# Bumping it is a deliberate step: bump, run `ruff format .`, and land the
# reformat as its own commit.
ruff==0.15.12

# Static analysis used by tests/test_lumaviewpro_app_smoke.py to catch
# undefined-name regressions in lumaviewpro.py — pre-2026-05-04 a
# mechanical refactor (LVP-A-3 partial) deleted module-level globals
# referenced inside App.build(). All 1713 tests passed but production
# startup NameError'd. pyflakes catches this class of bug at static-
# analysis speed (<100ms) without needing a Kivy headless harness.
pyflakes==3.4.0

# Property-based testing for the video cadence selector and frame-budget
# arithmetic (tests/test_video_cadence.py). Dev/CI only; the test module
# loads a derandomized profile when CI is set so CI runs are reproducible.
hypothesis==6.165.2

# Profiling — see Firmware/docs/PROFILING_PRACTICE.md
# py-spy: sampling profiler, attaches to live LVP process without restart,
# low overhead so it can run for hours without warping timing data.
py-spy>=0.4
ome-types==0.6.3
